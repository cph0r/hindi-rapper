# Hugging Face Spaces Configuration

# Use this to configure the space environment
# Uncomment and modify as needed

# Python version
# python_version: "3.10"

# Gradio version (specified in requirements.txt)
# sdk_version: "4.0.0"

# Prebuild command (runs before starting the app)
# prebuild: "bash setup.sh"

# Startup command (if different from default)
# startup: "python app.py"

